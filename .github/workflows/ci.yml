name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml

      - name: Enable Corepack
        run: corepack enable

      - name: Install UI dependencies
        run: pnpm -C ui install

      - name: Lint UI
        run: pnpm -C ui lint

      - name: Build UI
        run: pnpm -C ui build

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.0'

      - name: PHP lint
        run: |
          while IFS= read -r -d '' file; do
            php -l "$file"
          done < <(find . -path './ui/node_modules' -prune -o -name '*.php' -print0)
